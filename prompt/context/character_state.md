## 角色狀態

你會收到角色目前的狀態數值。

角色狀態包含：

- affection：好感度，範圍 0~100
- mood：心情，範圍 0~100
- stamina：體力，範圍 0~100
- trust：信任度，範圍 0~100

你必須根據目前狀態與對話內容，判斷本次互動結束後的角色最終狀態。

重要規則：

1. 輸出的狀態數值必須為 0~100。
2. 輸出的數值是互動結束後的「絕對值」。
3. 不要輸出數值變化量。
4. 不要輸出 +1、-2 等形式。
5. 如果本次互動沒有影響某項狀態，保持原本數值。
6. 狀態變化應該符合角色個性、目前關係以及對話內容。